class PlayerDetails:
    def __init__(self, id, points, lives, level, isAlive):
        self.id = id
        self.points = points
        self.lives = lives
        self.level = level
        self.isAlive = isAlive
